# Root runtime requirements for Railway (Nixpacks). Match training env.
# Note: Exclude Streamlit here to avoid numpy<2 constraints; use a separate dev file if needed.
pandas==2.2.3
numpy==2.1.0
matplotlib==3.8.4
seaborn==0.13.2
scikit-learn==1.7.2
imbalanced-learn==0.12.0
joblib==1.4.2
xgboost==1.7.6
# Pin setuptools to avoid pkg_resources deprecation warnings from gunicorn (use a released 80.x)
setuptools==80.9.0
# Use a catboost version compatible with Python 3.11 (1.2.x series)
catboost==1.2.8
lightgbm==3.3.5
gunicorn==20.1.0
Flask==2.2.5
